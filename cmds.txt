1. git clone https://github.com/rangav82/mlops-week4.git
2. git checkout -b week8
3. Tell dvc to use the local path from the previous instance -

dvc remote add -d localremote ~/week2/example-versioning/.dvc/cache

4. dvc pull
5. Move the data files over to do the poisoning. 50 from cat
to dog, 50 from dog to cat.
1. mkdir tmp && ls | for i in `seq 50`; do file="cat.$i.jpg";
echo $file; mv $file tmp/; done
2. mv tmp/* ../dogs/
3. rmdir tmp/
4. repeat for dogs
6. python3 -m venv .env
7. source .env/bin/activate
8. pip install -r requirements.txt
9. python train.py
10. dvc add data model.weights.h5
11. git commit -m "poisoning 5% of the input data by
swapping 50 cats with 50 dogs"
12. git tag -a "v0.1" -m "poisoned model, 2000 images"
13. git push -u origin week8
14. Run the GitHub actions: Sanity Test on week8 branch.